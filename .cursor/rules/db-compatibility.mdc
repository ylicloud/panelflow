---
description: [核心] 历史数据库兼容规则
globs: "**/*.cs"
alwaysApply: false
---

# 历史数据库兼容规则

## 基本原则

- 本项目**数据库与原有系统共用**，必须严格保护现有表结构。
- 功能使用现代 C# 方式重新实现，但**不得破坏历史数据兼容性**。
- “数据兼容优先于代码优雅”。
- 本项目使用SQL Server 2022数据库。

## 禁止事项

- 随意删除或重命名历史表中的关键字段（如 `fabh`、`x_mc`、`x_dj` 等）。
- 改变金额、数量、日期等字段的精度和格式。
- 破坏主键、唯一约束和外键关系。

## 正确做法

- 在实体类中允许保留历史字段名以兼容数据库。
- 新增字段必须使用可空类型或提供合理默认值。
- 所有数据库映射统一在 `OnModelCreating` 或配置类中声明。
- 涉及结构变更时，必须提供映射关系和回归验证方案。
- 批量操作必须使用事务，并对历史脏数据进行防御性处理。

## 密码兼容性

- 用户密码验证**必须**使用 `PbCryptoHelper`（实现 `IPasswordCryptoService`），不得替换为 BCrypt、SHA256 等其他算法。
- 原因：密码哈希由历史 PowerBuilder 系统写入，替换算法将导致所有历史用户无法登录。

**目标**：在新系统中正常使用历史数据，避免因重构导致数据损坏或统计错误。
