---
description: [核心] 业务生命周期与流程约束
globs:
  - "**/Controllers/**/*.cs"
  - "**/Services/**/*.cs"
  - "**/Models/**/*.cs"
alwaysApply: false
---

# 业务生命周期规则

## 核心业务流程

项目进度共 **8 个阶段**，按以下顺序执行，第7步可选：

1. **报价阶段**：导入客户BOM表(Excel) → 填写单价 → 生成报价单 → 发送客户
   - 客户确认 → 继续下一阶段
   - 客户不认可 → 项目终止归档（`cur_status=4`）
2. **签订制造合同**：合同审批、金额确认
3. **安排生产**：a.创建生产任务单，b.下达采购计划（基于BOM），c.指定装配小组，d.指定质检人员
4. **采购元件**：按计划下单 → 到货 → 验收 → 触发入库（写库存流水）
5. **控制柜装配**：触发出库/领料 → 装配 → 完成装配
6. **质量检验**：接收任务 → 检验 → 打印装配卡 → 质检完成
7. **用户带电**（**可选**）：申请 → 供电 → 调试 → 带电完成（部分客户在车间内带电，部分跳过）
8. **发货**：包装 → 运输 → 出厂（项目正式关闭，转入质保）

## 出厂边界

- **发货完成 = 项目进度关闭**，出厂后一律进入**质保期管理模块**。
- 现场安装、现场调试等出厂后事项，不属于项目进度流，由质保模块记录。

## 独立支撑模块（非进度阶段）

- **库存管理**：入库 / 出库 / 盘点 / 调拨，由采购入库和装配领料触发，也支持手动操作。
- **质保期管理**：3年质保期跟踪、故障报修、维修处理记录、质保到期关闭。

## 关键约束（必须遵守）

- **禁止跳阶段**：报价未确认前不得进入合同；合同未签订前不得下达采购计划。
- **质检未通过，不得发货出厂。**
- 所有状态变更和回退操作必须记录原因和操作人（写 `proj_status_audit`）。
- 客户BOM(Excel)导入后必须与报价单建立可追溯关系，并贯穿至采购计划和领料单。
- 金额、数量等关键字段计算口径必须全流程统一。
- 第7步"用户带电"可由操作人标记跳过，跳过原因须记录。

## 权限要求

- 关键节点（合同签订、金额确认、出厂放行）必须有角色权限控制（`[RoleAuthorize]`）。

**原则**：任何涉及业务流程的修改，必须符合上述生命周期约束。
