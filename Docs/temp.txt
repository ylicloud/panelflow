(1) **NormalizeSpec**, 这个c#端函数, 处理字符串的结果, 是否与sql server中F_cleanString函数处理结果能确保完全相同吗? 是不是c#直接调用数据库端的函数更容易保持一致性?

(2) 报价员如果想查看某个元件的平均价格\最大\最小价格, 用哪种方式合适? 右键单击表格中元件序号? 还是加个链接或按钮?

(3) 增加自动检测价格的功能: 当某个价格为0,或者为负,或明显偏离历史报价超过某个数值(比如20%)时, 可以用某种方式提醒报价员,比如消息或颜色.

(4) 价格为负数时, 必须修正为0或正值,才能保存入库, 确保数据干净.

(5) 表格中, 除了现有列, 再增加1列:金额,由计算得出来,计算方式为 (x_bj_dj*(1+x_fdds/100))*x_sl, 其中x_bj_dj为单价,x_fdds为浮动点数(即系数),x_sl为数量.

(6) 写入方案到数据表时,字段x_dj由公式计算得到:=x_bj_dj*(1+x_fdds/100);
(7) 待解决问题: 元件及控制柜清单, 从数据库读取出来之后, 用户可以增加/修改/删除控制柜, 也可以增加/修改/删除控制柜内元件. 那么, 是不是把读入页面中的数据与数据库中数据完全独立? 当用户保存数据时, 会把数据库中此方案的所有元件都删除,重新写入最新的元件清单? 因为元件编号是递增的,数量有增减时编号就变了.
(8) 引出另一个问题: 已经具有的功能是: 导入excel,也就是把元件清单导入数据库中. 那么, 对元件或控制柜的增删,是放在报价功能中, 还是放在excel导入的功能中? 你帮我想想怎么设计比较合理?

(9) 单击树形目录根节点时,显示所有元件的汇总(只读), 类似数据库中的 group by x_mc, x_ggxh, x_dj,sum(x_sl) ,能做到吗?
(10) 修改元件价格后,当前方案号中有相同规格型号指纹特征的元件,价格同时修改.